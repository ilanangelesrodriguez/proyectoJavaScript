//Notacion
console.log("Init :v");
//;

//. --> se utiliza en los objetos para acceder a los atributos

//movil.anchura

//[] -->Para listas, arreglos o arrays
const arreglo=[12,08,20,02];
console.log(arreglo[2]);

//() -->Para funciones
function suma(a,b){
    //funcion xd
}

//{} -->Para objetos, funciones y estructuras de control
const movil={
    anchura:5,
    altura:12
}
if (true) {
    //todo lo que haya entre llaves
}
